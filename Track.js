class Track {
  constructor(trackNumber, fileName, duration, source) {
    this.trackNumber = trackNumber;
    this.fileName = fileName;
    this.duration = duration;
    this.source = source;
  }
}

export { Track };
